@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Herstelcode verificatie";
}
<div class="container">
    <h2>@ViewData["Title"]</h2>
    <hr />
    <p>
        Je hebt verzocht om met een herstelcode in te loggen. Deze login zal niet worden onthouden totdat u een authenticatie app code voorziet
        bij de login of 2FA uitschakelt en opnieuw inlogt.
    </p>
    <div class="row">
        <div class="col-md-4">
            <form method="post">
                <div asp-validation-summary="All" class="text-danger"></div>
                <div class="form-group">
                    <label asp-for="Input.RecoveryCode">Herstelcode</label>
                    <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" />
                    <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-default">Log in</button>
            </form>
        </div>
    </div>
</div>

 
 @section Scripts {
    <partial name="_ValidationScriptsPartial" />
}